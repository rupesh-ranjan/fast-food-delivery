/* eslint-disable no-undef */
import { render } from "@testing-library/react";
import ContactForm from "../ContactForm";

test("Should load contact form", () => {
  render(<ContactForm />);
  //const heading = screen.getByRole("heading");
});
